# Kustomization for observability releases
# Watches manifests/releases/observability/ and applies HelmReleases
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: releases-observability
  namespace: flux-system
spec:
  interval: 10m
  retryInterval: 1m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  path: ./manifests/releases/observability
  prune: true
  wait: true
  dependsOn:
    - name: releases-security

