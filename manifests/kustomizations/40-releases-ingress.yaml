# Kustomization for ingress releases
# Watches manifests/releases/ingress/ and applies HelmReleases
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: releases-ingress
  namespace: flux-system
spec:
  interval: 10m
  retryInterval: 1m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  path: ./manifests/releases/ingress
  prune: true
  wait: true
  dependsOn:
    - name: releases-system

